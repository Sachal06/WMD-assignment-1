var per_son = "Sachal";
console.log(per_son.toLowerCase());
console.log(per_son.toUpperCase());
var titlecase_person_name = per_son.charAt(0).toUpperCase() + per_son.substring(1).toLowerCase();
console.log(titlecase_person_name);
